{
  "devDependencies": {
    "@types/node": "^17.0.32",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "dev":"tsc -p .",
    "start": "ts-node-dev  --cache-directory --respawn --transpile-only ./Decorators/decorators.ts",
    "build": "tsc --build --clean && tsc"
  },
  "dependencies": {
    
    "ts-node": "^10.7.0"
  }
}
